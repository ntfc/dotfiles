#
# These things are run when an Openbox X Session is started.
#

[ -f ~/.scripts/openbox/setlayout ] && (~/.scripts/openbox/setlayout 0 2 2 0) &

# systray, clock, all dock-able apps
(sleep 1.0s && ~/.scripts/exec-dzen2) &
# dzen2 provides a clock
#(sleep 1.0s && stalonetray -c ~/.config/stalonetrayrc) &
(sleep 1.5s && lal -b --color "#000000" -s 14 -f " %a %d  %H:%M:%S" -w 130 -t "Droid Sans") &

