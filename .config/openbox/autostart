#
# These things are run when an Openbox X Session is started.
#

[ -f ~/.scripts/openbox/setlayout ] && (~/.scripts/openbox/setlayout 0 2 2 0) &

# this is still not in the right order.....
# clock
#(sleep 1s && ~/.scripts/exec-clock) &
# systray
(sleep 0.1s && stalonetray -c ~/.config/stalonetrayrc) &
(sleep 0.5s && lal -f " %a %d  %H:%M:%S " -b --font "Inconsolata" -s 16 -w 160) &
# dzen2 statusbar
#(~/.scripts/exec-dzen2) &
